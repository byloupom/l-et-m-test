import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
  palette: {
      text: {
        primary: '#1f1f28',
        secondary: '#958BAD'
      }
    },
  typography: {
    fontWeightBold: 900
  },
  });